# storage

Skills for putting files into object/blob storage.

- **[object-storage-upload](./object-storage-upload/SKILL.md)** — upload/back up local files
  to S3, Cloudflare R2, GCS, Azure Blob, MinIO, and 40+ backends via the unified
  [files-sdk](https://files-sdk.dev). Pairs with `retrieval/manualslib-fetch` to archive
  fetched manuals.
